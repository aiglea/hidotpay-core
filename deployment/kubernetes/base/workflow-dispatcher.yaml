apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-dispatcher
  namespace: hidotpay-financial
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-dispatcher
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workflow-dispatcher
    spec:
      serviceAccountName: workflow-dispatcher
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: workflow-dispatcher
      terminationGracePeriodSeconds: 30
      containers:
        - name: dispatcher
          image: ghcr.io/hidotpay/workflows:0.2.42
          imagePullPolicy: IfNotPresent
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          envFrom:
            - secretRef:
                name: hidotpay-workflow-dispatcher
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 2
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: workflow-dispatcher
  namespace: hidotpay-financial
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-dispatcher
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: workflow-dispatcher
  namespace: hidotpay-financial
spec:
  minReplicas: 3
  maxReplicas: 12
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: workflow-dispatcher
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: workflow-dispatcher-private-egress
  namespace: hidotpay-financial
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: workflow-dispatcher
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - namespaceSelector:
            matchLabels:
              hidotpay.io/plane: infrastructure
      ports:
        - protocol: TCP
          port: 9092
        - protocol: TCP
          port: 7233
