# 更新紀錄

## 0.2.58 — 2026-08-15

- 修正 Figma 錢包引導頁在極短螢幕可能截斷「開始使用」按鈕的問題；畫面現在可垂直捲動，保留原有登入流程與視覺設計。
- 此版僅更新公開靜態使用者入口，不含 API、資料庫、私鑰或任何資金功能；充值、轉帳與提現開關均未變更。

## 0.2.57 — 2026-08-15

- 依 Figma 更新未登入錢包首次引導頁，公開 Worker 僅部署 Expo 匯出的 SPA 靜態資產，支援 `/callback` 路由回到 App。
- App、根專案與鎖定檔版本同步升至 0.2.57；靜態 UI 不內嵌 API、資料庫、Token 或私鑰。
- 這不是資金功能上線：未設定受保護 API 時不顯示假餘額，充值、轉帳與提現後端安全開關維持不變。

## 0.2.56 — 2026-08-15

- 已完成充值鏈重組 P0 程式修正：掃描器只在安全水位後入帳；深度重組或歷史 `orphaned + receipt` 一律停止受影響網路掃描，不會自動扣減使用者、寫反向分錄或改寫不可變總帳。
- 新增繁中深度重組事故處置手冊：保存 RPC／區塊證據，逐筆對帳 receipt、不可變總帳與 Blnk，僅能在兩名不同職責覆核人簽核後恢復掃描，並明確禁止手動修改總帳。
- 專用 `hidotpay_test` 真實資料庫驗證仍是發布關卡；資金與提現開關持續關閉。本版本不推送、不部署，也不表示已具備真實資產上線資格。

## 0.2.55 — 2026-08-15

- 最終生產審查發現並隔離充值鏈重組 P0：舊流程可能把已入帳觀測標記為失效，但沒有對帳本餘額作安全處理。此版本定稿「安全水位後才入帳、深度重組立即停機、絕不自動扣回使用者餘額」的修正設計；在程式與測試完成前不會推送或開啟任何資金功能。

## 0.2.54 — 2026-08-15

- 新增登入者專屬的錢包交易紀錄：服務端只以登入身分導出可用帳戶，交易資料採穩定游標分頁並保留微秒排序精度；回應僅包含交易編號、類型、資產、正整數最小單位金額、收支方向與時間，絕不回傳對手方、付款資料、地址、雜湊或帳本 metadata。
- 使用者 App 現在可顯示載入中、空白、錯誤重試、交易列表與載入更多；未知類型會中性顯示，未設定受保護 API 時仍不顯示假資料。窄螢幕渲染已修正背景溢出並實測可垂直捲動。
- 專用 CockroachDB 測試網址不存在時，整合測試只針對「資料庫不存在」安全跳過；其他連線失敗仍會報錯，避免誤把環境故障當成成功。
- 真實資產與提現開關持續關閉；正式 Logto API Resource、精確正式 CORS 網域、隔離簽名器、資料保護、雙人覆核及測試網端到端演練仍是必要上線門檻。

## 0.2.53 — 2026-08-15

- 定稿使用者專屬交易紀錄設計：只允許登入者透過 `/v1/me/transactions` 查閱自己的可用錢包帳本流水，採游標分頁、伺服器判定入／出帳，且不回傳對手方、付款資料、私鑰或內部風控資訊。
- 明確保留真實資產開關、正式 Logto API Resource、精確 CORS 網域、隔離簽名器與正式資料保護為獨立上線門檻；此版本不啟用任何資金流程。

## 0.2.52 — 2026-08-15

- 使用者 App 從登入測試殼升級為錢包首頁：登入後可安全讀取餘額、取得 Ethereum／TRON 獨立充值地址，以及提交 USDT 站內轉帳；每個呼叫都要求登入者的 Logto access token 與冪等鍵。
- 新增「未設定受保護 API 時不顯示假資產」的狀態頁、轉帳格式驗證、空餘額／讀取失敗狀態與公開環境設定範本；App 不保存私鑰、服務密鑰或資料庫連線字串，並拒絕將登入憑證傳至 HTTP API。
- Web API 入口新增嚴格 CORS 網域白名單與 OPTIONS 預檢處理；只接受設定中完全一致的來源，成功與錯誤回應都會帶必要 CORS 標頭。
- Figma 連線帳號尚未能讀取指定圖層，因此本版不宣稱像素級還原；交易紀錄、P2P 介面及實際 API Resource 驗收仍待完成。帳本與提現開關維持關閉。

## 0.2.51 — 2026-08-15

- 新增可重跑的 NocoBase 後台投影工作者：只讀取 CockroachDB 指定 `admin_*` 檢視表的白名單欄位，再以冪等方式寫入六個可重建投影集合；帳本與資產開關均未改動。
- 建立並實測兩個最小權限後台角色：投影服務僅可寫入投影集合；財務檢視者僅可查詢投影。兩者都不能讀取使用者、管理設定、建立資料來源或接觸簽名材料。
- 新增私有 Kubernetes CronJob、唯讀 CA Secret 掛載與容器建置驗證；正式環境仍要求私有 HTTPS NocoBase。資金與提現開關持續關閉，尚未宣告正式保管上線。

## 0.2.50 — 2026-08-15

- 確認 NocoBase 開源版不含外接 CockroachDB 的資料來源模組，改採「帳本唯一真相、後台可重建唯讀投影」設計，避免後台取得資金資料庫寫入能力。
- 本機 NocoBase 已以僅限本機連線的容器正常啟動，預設管理者密碼已改為本機系統鑰匙圈保存的隨機密碼。
- 實測 CockroachDB 的後台讀取帳號只可查詢指定 `admin_*` 檢視表，沒有錢包、總帳與提款來源表的寫入權限。

## 0.2.49 — 2026-08-15

- 新增第一階段完成度與正式上線缺口總表，將已測試程式能力、封閉候選環境與仍需外部驗收的託管門檻清楚分開。
- 將下一個可交付工作固定為 NocoBase 唯讀財務後台的可重現正式化驗收；資金開關維持關閉。
- 補齊專用 CockroachDB 測試帳號的既有與未來資料表權限設定，避免新 P2P migration 造成整合測試帳號權限不足；九項實際資料庫整合測試均已通過。

## 0.2.48 — 2026-08-15

- 修正 CockroachDB 線上欄位回填期間的遷移相容性：遷移程式會逐條執行 SQL，避免將結構調整與資料回填混在同一筆資料庫呼叫而被拒絕。
- P2P 付款期限遷移改為可安全續跑，並保留既有部署的校驗碼相容性；新安裝的後台訂單檢視表會把新欄位附加在末端，既有安裝則保留原欄位順序以避免破壞低代碼後台相容性。
- 已在獨立 `hidotpay_test` CockroachDB 完成六項真實驗收：內部轉帳、充值／提現帳本、併發防超支、地址分配、P2P 託管與逾時退款均通過。

## 0.2.47 — 2026-08-15

- 專用 CockroachDB 整合測試現在涵蓋 P2P 託管鎖定、放幣、重送與逾時退款，避免只驗證一般轉帳而遺漏 P2P 資金不變量。

## 0.2.46 — 2026-08-15

- 修正 CockroachDB 會自動命名舊版欄位限制所造成的 P2P 遷移相容性問題；新遷移會安全移除舊限制後恢復允許託管帳戶與 P2P 帳本類型的限制。
- 新增回歸測試，避免未來版本再次因資料庫限制名稱不同而使 P2P 託管帳戶建立失敗。

## 0.2.45 — 2026-08-15

- 新增 Cloudflare Workers 原生 Hyperdrive 路由：錢包、站內轉帳、P2P 廣告／訂單／仲裁、收款方式與多鏈充值地址皆維持登入與資金開關保護。
- 收款資料僅經 OpenBao Transit 加密，充值地址僅經隔離簽名服務衍生；Worker 不保存或回傳私鑰、助記詞與原始收款帳號。
- 補齊正式啟用門檻與回滾文件；提款與資金開關預設保持關閉，直到隔離資料庫、Logto、OpenBao、簽名器與對帳演練完成驗收。

## 0.2.44 — 2026-08-15

- P2P 訂單會保存付款截止時間；逾時且尚未付款時，私有排程以可重送、雙式記帳退款給賣方。
- 新增最小權限 Kubernetes CronJob，禁止與前一次工作重疊，且只能連至 CockroachDB。
- App 顯示付款期限，避免買賣雙方對自動退款時間沒有資訊。

## 0.2.43 — 2026-08-15

- 加入 P2P 廣告、下單、獨立託管帳戶、付款標記、放幣、退款、爭議與仲裁 API。
- 每一筆 P2P 鎖定、放幣與退款都建立雙式帳本流水；同一請求重送不會重複移動資產。
- NocoBase 後台僅可透過唯讀檢視表查核 P2P 訂單與爭議，不可直接改帳或接觸簽名材料。
- 補齊 P2P OpenAPI 與 CockroachDB 整合測試案例；主網提現與真實託管仍維持安全關閉，直到外部驗收完成。

## 0.1.1 — 2026-08-13

- 修正網頁登入完成後無法返回首頁的問題。
- 修正登出使用錯誤回跳網址而被 Logto 拒絕的問題。
- 加入登入回跳、登出回跳與版本一致性的自動檢查。

## 0.1.0 — 2026-08-12

- 建立 hidotpay 網頁登入流程驗證 App。
